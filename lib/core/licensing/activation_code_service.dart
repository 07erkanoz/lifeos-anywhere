/// Activation code service — stub, app is free.
class ActivationCodeService {
  ActivationCodeService._();

  static String? normalise(String input) => null;
  static bool isValid(String input) => false;
  static String format(String code) => code;
}
